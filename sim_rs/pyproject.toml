[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "sim_rs"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
bindings = "pyo3"
module-name = "sim_rs"
features = ["pyo3/extension-module"]
